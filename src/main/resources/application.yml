spring:
  datasource:
    url: jdbc:mysql://endpoint.amazonaws.com:3306/user
    username: admin
    password: twotwo22
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    defer-datasource-initialization: true
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true

  security:
    oauth2:
      client:
        registration:
          github:
            client-id:
            client-secret:

  jwt:
    secret:

